# Autoapply extension

Load unpacked in Chrome from this folder. The content script adds **Apply with my profile** on any page and POSTs the URL to the local Autoapply API (`http://127.0.0.1:8787/api/jobs`).
